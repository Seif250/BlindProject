[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --prefer-offline --no-audit"]

[phases.build]
cmds = ["cd server && echo 'Build complete'"]

[start]
cmd = "cd server && npm start"

[variables]
NODE_ENV = "production"
